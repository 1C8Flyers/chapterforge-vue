version: "3.9"

services:
  chapterforge:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - ./chapterforge.db:/app/chapterforge.db
    restart: unless-stopped
